# @creationflow/database

Prisma database package for CreationFlow.

The schema is prepared for workspace-scoped product customization data. It does not include license server tables, billing tables, seed data, or customer examples.
